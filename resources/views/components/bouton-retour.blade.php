@props(['route'])
<a href="{{ $route }}" class="btn btn-sm btn-icon btn-outline-secondary flex-shrink-0" title="Retour à la liste">
    <i class="bi bi-arrow-left"></i>
    <span class="visually-hidden">Retour à la liste</span>
</a>
